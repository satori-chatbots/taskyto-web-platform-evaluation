name: calculate_total
kind: action
data:
  - equipment_type:
      type: enum
      values:
        - laptop
        - desktop
        - workstation
        - mini_pc
  - quantity:
      type: integer

on-success:
  execute:
    language: python
    code: |
      prices = {
          "laptop": 900,
          "desktop": 1200,
          "workstation": 2000,
          "mini_pc": 700
      }
      total = prices[equipment_type] * quantity
      return str(total)

  response:
    text: "The total cost for {quantity} {equipment_type}(s) is ${result}."
    rephrase: simple